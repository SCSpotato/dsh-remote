# 复制本文件为 keystore.properties 并填入你自己的签名信息。
# keystore.properties 已被 .gitignore 忽略，千万不要提交它（含私钥密码）。
#
# storeFile 相对 app/ 模块目录解析（也可以填绝对路径）。
storeFile=C:/path/to/your/keystore.jks
storePassword=change-me
keyAlias=your-alias
keyPassword=change-me
